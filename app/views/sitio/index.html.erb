<section id="preguntas" class="span-10">
	<h2>Preguntas</h2>
	<ul>
		<% @preguntas.each do |pregunta| %>
		<li>
			<article class="pregunta">
				<section class="titulo">
					<h4>
						<%= link_to pregunta.titulo, mostrar_pregunta_path(pregunta.id, pregunta.slug), {:title => strip_tags(pregunta.texto)} %>
					</h4>
				</section>
				<section class="datos">
					<ul>
						<li>
							Preguntado por: <%= link_to pregunta.usuario.login, mostrar_usuario_path(pregunta.usuario.id, pregunta.usuario.login) unless pregunta.usuario.nil? %>
						</li>
						<li>
							<%= pregunta.mostrar_fecha %>
						</li>
					</ul>
				</section>
				<section class="estadisticas">
					<ul>
						<li>
							Vistas: <%= pregunta.vistas %>
						</li>
						<li>
							Votos: <%= pregunta.votos %>
						</li>
						<li>
							Respuestas: <%= pregunta.respuestas.count %>
						</li>
					</ul>
				</section>
				<section class="etiquetas">
					<ul class="lista_etiquetas">
						<% pregunta.etiquetas.each do |etiqueta| %>
						<li>
							<%= link_to etiqueta.nombre, mostrar_etiqueta_path(etiqueta.id, etiqueta.slug) %>
						</li>
						<% end %>
					</ul>
				</section>
				
			</article>
		</li>
		<% end %>
	</ul>
</section>
<section id="codigos" class="span-10">
	<h2>Códigos</h2>
	<ul>
		<% @codigos.each do |codigo| %>
		<li>
			<section class="titulo">
				<h4><%= link_to codigo.titulo, mostrar_codigo_path(codigo.id, codigo.slug) %></h4>
			</section>
			<section class="descripcion">
				<%= codigo.descripcion %>
			</section>
			<section class="datos">
				<ul>
					<li>
						Subido por: <%= codigo.usuario.login unless codigo.usuario.nil? %>
					</li>
					<li>
						<%= codigo.mostrar_fecha %>
					</li>
				</ul>
			</section>
			<section class="estadisticas">
				<ul>
					<li>
						Vistas: <%= codigo.vistas %>
					</li>
					<li>
						Descargas: <%= codigo.descargas %>
					</li>
					<li>
						Votos: <%= codigo.votos %>
					</li>
				</ul>
			</section>
			<section class="etiquetas">
				<ul>
					<% codigo.etiquetas.each do |etiqueta| %>
					<li>
						<%= link_to etiqueta.nombre, mostrar_etiqueta_path(etiqueta.id, etiqueta.slug) %>
					</li>
					<% end %>
				</ul>
			</section>
		</li>
		<% end %>
	</ul>
</section>
<section class="span-4 last">
	<section id="etiquetas_destacadas">
		<h3>Etiquetas destacadas</h3>
		<ul class="lista_etiquetas">
			<% @etiquetas_destacadas.each do |etiqueta| %>
			<li>
				<%= link_to etiqueta.nombre, mostrar_etiqueta_path(etiqueta.id, etiqueta.slug) %>
			</li>
			<% end %>
		</ul>
		<%= link_to 'Ver todas las etiquetas', etiquetas_path %>
	</section>

	<section id="publicidad">
		<script type="text/javascript"><!--
			google_ad_client = "ca-pub-5471977035368249";
			/* portalprogramadores120x240 */
			google_ad_slot = "4182082325";
			google_ad_width = 120;
			google_ad_height = 240;
			//-->
			</script>
			<script type="text/javascript"
			src="http://pagead2.googlesyndication.com/pagead/show_ads.js">
			</script>
	</section>
</section>

<div class="clear"></div>